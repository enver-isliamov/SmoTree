
# 🚜 SmoTree Refactoring Roadmap

Этот документ отслеживает процесс рефакторинга системы ролей и прав доступа.

## 🏁 Цель: Упрощение системы ролей
Переход от 4 ролей к 3 понятным уровням доступа: **Admin**, **Creator**, **Guest**.

### Определения Ролей

1.  **Admin (Владелец)**
    *   Зарегистрированный пользователь (Google), который создал проект или вошел в систему без приглашения.
    *   *Права:* Полный доступ ко всем проектам, создание проектов, удаление любых данных.

2.  **Creator (Коллаборатор)**
    *   Авторизованный юзер (Google), который получил приглашение к проекту через ссылку.
    *   Работает в проекте не как гость, а под своим авторизованным именем.
    *   *Права:* Может загружать версии, управлять статусами комментариев, но видит в Дашборде только те проекты, в которые добавлен.

3.  **Guest (Гость)**
    *   Пользователь, который получил ссылку и не авторизовался через Google (вошел вручную).
    *   *Права:* Только чтение и комментирование. Не может удалять файлы, экспортировать маркеры или видеть другие проекты.

---

## 📋 План Действий

### 1. Фундамент (Types & Constants)
- [x] Обновить `UserRole` enum в `types.ts`.
- [x] Обновить `MOCK_USERS` в `constants.ts` с новыми ролями.

### 2. Дашборд (Dashboard Logic)
- [x] **Фильтрация проектов:**
    - Admin: Видит все доступные проекты (включая чужие).
    - Creator/Guest: Видят только проекты, где они в команде.
- [x] **UI Элементы:**
    - Guest: Скрыть кнопку "+ New Project".
    - Guest: Заголовок "Shared with Me".
- [x] **Права на удаление:**
    - Admin: Может удалить любой проект.
    - Creator: Может удалить только проект, где он `owner`.
    - Guest: Запрещено удаление.

### 3. Просмотр Проекта (Project View)
- [x] Скрыть кнопку `Upload Asset` для Guest.
- [x] Скрыть кнопку удаления ассета для Guest.

### 4. Плеер (Player Core)
- [x] **Управление:**
    - `canManageProject` = Admin || Creator.
- [x] **Комментарии:**
    - Guest: Может удалять/редактировать только свои.
    - Creator/Admin: Могут модерировать (удалять) любые комментарии.
    - Guest: Не видит чекбокс "Resolve".
    - Guest: Не видит кнопку "Resolve All".
- [x] **Визуал:**
    - [x] Бейджик "Guest" возле имени автора комментария.
- [x] **Экспорт:**
    - Guest: Меню экспорта (EDL/XML) скрыто.
    - Guest: Меню загрузки версий скрыто.

### 5. Аутентификация и Роутинг (Security)
- [x] **Guest Login:**
    - Вход по ссылке (`?projectId=...`) -> Ручной ввод -> Роль `GUEST`.
- [x] **Creator Login:**
    - Вход по ссылке (`?projectId=...`) -> Google Auth -> Роль `CREATOR`.
- [x] **Admin Login:**
    - Вход без ссылки -> Google Auth -> Роль `ADMIN`.
- [x] **Deep Link Protection:** Проверка прав при переходе по прямой ссылке.

## 🚀 Этап VII: SaaS Архитектура (Contextual Permissions)
**Статус:** 🏗 В работе

Переход от жестких ролей пользователей к правам, основанным на проекте.

- [x] **Login Refactor:** Google-юзер всегда получает статус аккаунта (`Admin`), но его права определяются контекстом проекта.
- [x] **Dashboard:** Удаление проекта доступно только его Владельцу (`ownerId`).
- [x] **Project View:** Кнопки управления (Upload/Delete) доступны Владельцу или членам команды (не Гостям).
- [x] **Player:** Права модератора (`canManage`) проверяются динамически (Владелец или Команда).
- [x] **Fix Race Condition:** Синхронизация потоков Login -> Join -> Fetch для корректного отображения приглашений.

---

## 🛠 Статус
**Текущий этап:** ✅ **Этап завершен.** Ролевая модель внедрена и работает в коде.

---

## 📝 Заметки на будущее (Info)
> _Этот блок служит справочной информацией для будущих архитектурных решений._

*   **Auth Migration:** Рассмотреть переход на **Clerk** (или Auth.js) для управления пользователями и сессиями. Это заменит текущую кастомную реализацию Google OAuth и упростит интеграцию с экосистемой Vercel.
